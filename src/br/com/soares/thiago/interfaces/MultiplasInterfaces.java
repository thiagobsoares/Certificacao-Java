package br.com.soares.thiago.interfaces;

import java.util.List;

public interface MultiplasInterfaces extends List {
	/*
	 * 
	 * Interface não pode implementar outras interfaces, 
	 * TODOS os metodos da interface devem ser sobreescritos
	 * 
	 * 
	 * 
	 * Which of the following statements is/are true? Select 1 option(s):
	 * Subclasses must define all the abstract methods that the superclass
	 * defines. A class implementing an interface must define all the methods of
	 * that interface. A class cannot override the super class's constructor. It
	 * is possible for two classes to be the superclass of each other. An
	 * interface can implement multiple interfaces.
	 * 
	 */
}
