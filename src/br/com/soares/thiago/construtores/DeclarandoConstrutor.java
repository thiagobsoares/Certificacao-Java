package br.com.soares.thiago.construtores;

public class DeclarandoConstrutor {
	// Which of these statements are true?

	// All classes must explicitly define a constructor.
	// A default no args one will be provided if not defined any.

	// A constructor can be declared private.
	// This feature is used for implementing Singleton Classes.

	// A constructor can declare a return value.
	// Return value NO, only RETURN

	// A constructor must initialize all the member variables of a class.
	// All non-final instance variables get default values if not explicitly
	// initialized.

	// A constructor can access the non-static members of a class.
	// A constructor is non-static, and so it can access directly both the
	// static and non-static members of the class.

	// Constructors need not initialize *all* the member variables of the class.
	// A non-final member(i.e. an instance) variable will be assigned a default
	// value if not explicitly initialized.
}
